# 在项目启动之前从nacos读取配置文件.
spring:
  application:
    name: userservice
  profiles:
    active: dev #环境
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos地址
      config:
        file-extension: yaml #文件后缀
